import base64

# obj = bucket.Object('chip.jpg')
# response = obj.get()
# body = response['Body'].read()
# b64 = base64.b64encode(body).decode('utf-8')
# img = "data:image/png;base64," + b64;

# pdf_obj = bucket.Object(pdf_key)
# response = pdf_obj.get()
# pdf_body = response['Body'].read()
# b64_pdf = base64.b64encode(pdf_body).decode('utf-8')
# pdf = "data:application/pdf;base64," + b64_pdf